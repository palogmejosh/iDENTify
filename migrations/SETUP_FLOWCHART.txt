╔════════════════════════════════════════════════════════════════════╗
║        iDENTify Database Setup - Decision Flowchart               ║
╚════════════════════════════════════════════════════════════════════╝


                        START HERE
                             ↓
         ┌───────────────────────────────────────┐
         │  Is this a NEW device installation?  │
         └───────────────────────────────────────┘
                   │                │
                  YES              NO
                   │                │
                   ↓                ↓
     ┌──────────────────────┐  ┌────────────────────────┐
     │  Fresh Installation  │  │  Existing Database     │
     │  (No database yet)   │  │  (Already has data)    │
     └──────────────────────┘  └────────────────────────┘
                   │                │
                   ↓                ↓
     ┌──────────────────────┐  ┌────────────────────────┐
     │  OPTION A: Automated │  │  Run ONLY:             │
     │  Double-click:       │  │  alter_database_       │
     │  setup_database.bat  │  │  setup_fixed.sql       │
     └──────────────────────┘  └────────────────────────┘
                   │                │
                  OR                │
                   │                │
                   ↓                │
     ┌──────────────────────┐      │
     │  OPTION B: Manual    │      │
     │  Run in order:       │      │
     │  1. database_setup   │      │
     │     .sql             │      │
     │  2. alter_database_  │      │
     │     setup_fixed.sql  │      │
     └──────────────────────┘      │
                   │                │
                   └────────┬───────┘
                            ↓
              ┌──────────────────────────┐
              │  Verification            │
              │  Run: SHOW TABLES;       │
              │  Should see 12 tables    │
              └──────────────────────────┘
                            ↓
                   ┌────────────────┐
                   │   SUCCESS! ✓   │
                   └────────────────┘


════════════════════════════════════════════════════════════════════


📋 FILE REFERENCE GUIDE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File                            Purpose                    When to Use
────────────────────────────────────────────────────────────────────
database_setup.sql              Base schema creation       Fresh install ONLY
alter_database_setup_fixed.sql  Adds enhancements          Always (after base)
alter_database_setup.sql        ❌ BROKEN - DO NOT USE    NEVER
setup_database.bat              Automated setup            Fresh install
README_SETUP.md                 Detailed instructions      Reference
QUICK_START.txt                 Quick reference            Quick lookup


════════════════════════════════════════════════════════════════════


⚠️  COMMON MISTAKES TO AVOID
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ Running alter_database_setup.sql (original file with errors)
✅ Use alter_database_setup_fixed.sql instead

❌ Running migrations before base setup
✅ Always run database_setup.sql first

❌ Forgetting to create the database first
✅ Create identify_db database before running scripts

❌ Running scripts in wrong order
✅ Follow the order: base → migrations


════════════════════════════════════════════════════════════════════


🎯 RECOMMENDED APPROACH FOR NEW DEVICE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

     1. Start XAMPP (Apache + MySQL)
            ↓
     2. Double-click: setup_database.bat
            ↓
     3. Wait for completion
            ↓
     4. Verify with: SHOW TABLES;
            ↓
     5. Done! Start using the system ✓


════════════════════════════════════════════════════════════════════


💡 PRO TIPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

• The fixed migration script is IDEMPOTENT
  (safe to run multiple times - it checks before adding)

• Keep the original alter_database_setup.sql for reference
  but NEVER run it

• If setup fails, DROP the database and start fresh:
  DROP DATABASE identify_db;

• Backup your database regularly after setup:
  mysqldump -u root identify_db > backup.sql


════════════════════════════════════════════════════════════════════

Questions? See: migrations/README_SETUP.md for detailed help
